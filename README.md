# Employees-Analysis
This Python code connects to a SQL Server database using SQLAlchemy and retrieves data from a table named 'Tab_Agent.' The script then performs various data manipulations and analyses using Pandas functions.
